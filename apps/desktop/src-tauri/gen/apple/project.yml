name: luma
options:
  bundleIdPrefix: dev.bwmp.luma
  deploymentTarget:
    iOS: 15.0
fileGroups: [../../src]
configs:
  debug: debug
  release: release
settingGroups:
  app:
    base:
      PRODUCT_NAME: Luma
      PRODUCT_BUNDLE_IDENTIFIER: dev.bwmp.luma
      CODE_SIGN_STYLE: Automatic
      DEVELOPMENT_TEAM: 828MX392ZN
targetTemplates:
  app:
    type: application
    sources:
      - path: Sources
    scheme:
      environmentVariables:
        RUST_BACKTRACE: full
        RUST_LOG: info
    settings:
      groups: [app]
targets:
  luma_iOS:
    type: application
    platform: iOS
    sources:
      - path: Sources
      - path: Shared
      - path: Assets.xcassets
      - path: ../../../../../branding/icon-composer/star.icon
      - path: Externals
        excludes:
          - "**/*.a"
      - path: luma_iOS
      - path: assets
        buildPhase: resources
        type: folder
      - path: LaunchScreen.storyboard
    info:
      path: luma_iOS/Info.plist
      properties:
        ITSAppUsesNonExemptEncryption: false
        LSRequiresIPhoneOS: true
        NSLocalNetworkUsageDescription: Luma uses your local network to connect to SSH servers and transfer files.
        NSSupportsLiveActivities: true
        UILaunchStoryboardName: LaunchScreen
        UIRequiredDeviceCapabilities: [arm64, metal]
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
    entitlements:
      path: luma_iOS/luma_iOS.entitlements
      properties: {}
    scheme:
      environmentVariables:
        RUST_BACKTRACE: full
        RUST_LOG: info
    settings:
      base:
        ENABLE_BITCODE: false
        MARKETING_VERSION: 0.18.0 # x-release-please-version
        CURRENT_PROJECT_VERSION: 1
        ASSETCATALOG_COMPILER_APPICON_NAME: star
        ARCHS: [arm64]
        VALID_ARCHS: arm64 
        LIBRARY_SEARCH_PATHS[arch=x86_64]: $(inherited) $(PROJECT_DIR)/Externals/x86_64/$(CONFIGURATION) $(SDKROOT)/usr/lib/swift $(DEVELOPER_DIR)/Toolchains/XcodeDefault.xctoolchain/usr/lib/swift/$(PLATFORM_NAME) $(DEVELOPER_DIR)/Toolchains/XcodeDefault.xctoolchain/usr/lib/swift-5.0/$(PLATFORM_NAME)
        LIBRARY_SEARCH_PATHS[arch=arm64]: $(inherited) $(PROJECT_DIR)/Externals/arm64/$(CONFIGURATION) $(SDKROOT)/usr/lib/swift $(DEVELOPER_DIR)/Toolchains/XcodeDefault.xctoolchain/usr/lib/swift/$(PLATFORM_NAME) $(DEVELOPER_DIR)/Toolchains/XcodeDefault.xctoolchain/usr/lib/swift-5.0/$(PLATFORM_NAME)
        ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES: true
        EXCLUDED_ARCHS[sdk=iphoneos*]: x86_64
        SWIFT_VERSION: '5.0'
        # ActivityKit is iOS 16.1+ and this target deploys to 15.0; without the weak
        # link dyld fails the app at launch on older systems instead of letting the
        # availability guards in LumaLiveActivity.swift take effect.
        OTHER_LDFLAGS: $(inherited) -weak_framework ActivityKit
      groups: [app]
    dependencies:
      - target: LumaLiveActivityExtension
        embed: true
      - framework: libapp.a
        embed: false
      - sdk: CoreGraphics.framework
      - sdk: Metal.framework
      - sdk: MetalKit.framework
      - sdk: QuartzCore.framework
      - sdk: Security.framework
      - sdk: UIKit.framework
      - sdk: WebKit.framework
    preBuildScripts:
      - script: |
          if command -v brew >/dev/null 2>&1; then
            export PATH="$(brew --prefix node@22)/bin:$HOME/.cargo/bin:$PATH"
          fi
          export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
          if [ -s "$NVM_DIR/nvm.sh" ]; then
            . "$NVM_DIR/nvm.sh"
            nvm use --silent default
          fi
          if [ "${CI_XCODE_CLOUD:-}" = "TRUE" ]; then
            "${SRCROOT:?}/ci_scripts/ci_build_rust.sh"
          else
            pnpm tauri ios xcode-script -v --platform ${PLATFORM_DISPLAY_NAME:?} --sdk-root ${SDKROOT:?} --framework-search-paths "${FRAMEWORK_SEARCH_PATHS:?}" --header-search-paths "${HEADER_SEARCH_PATHS:?}" --gcc-preprocessor-definitions "${GCC_PREPROCESSOR_DEFINITIONS:-}" --configuration ${CONFIGURATION:?} ${FORCE_COLOR} ${ARCHS:?}
          fi
        name: Build Rust Code
        basedOnDependencyAnalysis: false
        outputFiles:
          - $(SRCROOT)/Externals/x86_64/${CONFIGURATION}/libapp.a
          - $(SRCROOT)/Externals/arm64/${CONFIGURATION}/libapp.a
  LumaLiveActivityExtension:
    type: app-extension
    platform: iOS
    # Live Activities need 16.1 and ActivityContent(state:staleDate:) needs 16.2.
    deploymentTarget: '16.2'
    sources:
      - path: LumaLiveActivityExtension
      - path: Shared
    info:
      path: LumaLiveActivityExtension/Info.plist
      properties:
        CFBundleDisplayName: Luma
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_NAME: LumaLiveActivity
        PRODUCT_BUNDLE_IDENTIFIER: dev.bwmp.luma.LiveActivity
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: 828MX392ZN
        # Must track the app target: the App Store rejects an extension whose
        # version differs from its host app.
        MARKETING_VERSION: 0.18.0 # x-release-please-version
        CURRENT_PROJECT_VERSION: 1
        SWIFT_VERSION: '5.0'
        ENABLE_BITCODE: false
        SKIP_INSTALL: true
        ARCHS: [arm64]
        EXCLUDED_ARCHS[sdk=iphoneos*]: x86_64
    dependencies:
      - sdk: ActivityKit.framework
      - sdk: SwiftUI.framework
      - sdk: WidgetKit.framework
